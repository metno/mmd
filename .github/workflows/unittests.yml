
name: tests

on:
  push:
    branches:
      - master
  pull_request:

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1

    - name: run unit tests
      run: |
        docker-compose -f docker-compose.unittests.yml up
